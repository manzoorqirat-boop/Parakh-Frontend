# Force npm to install optional dependencies (native binaries like
# @tailwindcss/oxide-linux-x64-gnu, lightningcss-linux-x64-gnu) to work
# around npm's optional-dependency bug (npm/cli#4828).
include=optional
